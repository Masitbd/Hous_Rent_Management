<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @expense_item.title %>
</p>

<%= link_to 'Edit', edit_expense_item_path(@expense_item) %> |
<%= link_to 'Back', expense_items_path %>
